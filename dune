; (ocamllex lexer)

; (menhir
;  (flags (--explain))
;  (modules parser))

; (executable
;  (name lextest)
;  (libraries core yojson))

(ocamllex lexer)

(executables
 (names test)
 (libraries core)
 (preprocess
  (pps ppx_deriving.show)))